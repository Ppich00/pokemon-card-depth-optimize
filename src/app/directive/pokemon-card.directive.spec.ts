import { PokemonCardDirective } from './pokemon-card.directive';

describe('PokemonCardDirective', () => {
  it('should create an instance', () => {
    const directive = new PokemonCardDirective();
    expect(directive).toBeTruthy();
  });
});
